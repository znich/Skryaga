<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <by.laguta.skryaga.activity.preference.DatePreference
            android:key="@string/salaryDate"
            android:dialogTitle="@string/selectSalaryDate"
            android:title="@string/selectSalaryDate"
            android:defaultValue="20"/>

    <by.laguta.skryaga.activity.preference.DatePreference
            android:key="@string/prepaidDate"
            android:dialogTitle="@string/selectPrepaidDate"
            android:title="@string/selectPrepaidDate"
            android:defaultValue="20"/>

    <by.laguta.skryaga.activity.preference.SecureModePreference
            android:key="@string/secureMode"
            android:title="@string/secureModePreference"
            android:defaultValue="false"/>

    <by.laguta.skryaga.activity.preference.ShowDailySpendingPreference
            android:key="@string/showDailySpending"
            android:title="@string/showDailySpendingTitle"
            android:summary="@string/showDailySpendingPreference"
            android:defaultValue="false"/>

    <by.laguta.skryaga.activity.preference.TransactionsUpdatePreference
            android:title="@string/updatePreference"
            android:key="@string/transactionsProcessed"
            android:defaultValue="false"/>

    <by.laguta.skryaga.activity.preference.MakeBackupPreference
            android:title="@string/backupData"
            android:key="@string/createBackup"/>

    <by.laguta.skryaga.activity.preference.RestoreDBPreference
            android:title="@string/restoreData"
            android:key="@string/restoreBackup"/>


    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->
    <!--<EditTextPreference
        android:key="example_text"
        android:title="@string/pref_title_display_name"
        android:defaultValue="@string/pref_default_display_name"
        android:selectAllOnFocus="true"
        android:inputType="textCapWords"
        android:capitalize="words"
        android:singleLine="true"
        android:maxLines="1" />-->

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
<!--    <ListPreference
        android:key="example_list"
        android:title="@string/pref_title_add_friends_to_messages"
        android:defaultValue="-1"
        android:entries="@array/pref_example_list_titles"
        android:entryValues="@array/pref_example_list_values"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />-->

</PreferenceScreen>
